# Helm Lab Validation

Every lab and extension has been executed and validated against a live **k3d-helm-lab** cluster (Kubernetes v1.35.5, Helm v3.19.0).

During this validation process, several friction points were resolved, instructions were streamlined, and complete hint templates were added to make the exercises effortless to follow.

---

## Summary of Live Validation & Instruction Improvements

| Lab / Topic | Validation Outcome | Instructions & Hints Improved | Checkpoint Tag |
| :--- | :--- | :--- | :--- |
| **Lab 0: Chart Creation** | `helm create`, inspecting structure, stripping templates, configuring `Chart.yaml`, minimal values, and linting verified. | Added full Lab 0 guide with explanation of `version` vs `appVersion` and `.helmignore`. | `lab-00-start` |
| **Lab 1: First Chart** | `helm lint`, `helm template`, `helm install`, `kubectl rollout`, `port-forward`, and `curl` verified. Missing value break-and-recover tested. | Clarified file roles and output expectations. | `lab-01-complete` |
| **Lab 2: Release Lifecycle** | Revision tracking, `--set` overrides, rollback to revision 1, and timed-out upgrade failure with image pull error verified. | Documented revision mechanics and rollback behaviors. | `lab-02-complete` |
| **Lab 3: Values & Environments** | `values-dev.yaml` and `values-prod.yaml` created. Precedence rules and multi-release coexistence verified on the cluster. | Explicit guidance on `--reset-values` vs persistent `--set` flags. | `lab-03-complete` |
| **Lab 4: Template Logic** | `extraEnv` (`with` + `range`) and `resources` (`toYaml` + `nindent`) implemented. Verified inside Pod with `printenv`. | Added hint showing variable quoting and scope traversal (`$.Release.Name`). | `lab-04-complete` |
| **Lab 5: Helpers & Labels** | `_helpers.tpl` created with standard labels and resource names. Verified selector immutability and endpoint slice matching. | Expanded hints to include complete definitions for `labels`, `deploymentName`, and `serviceName` (`trunc 63 \| trimSuffix "-"`). | `lab-05-complete` |
| **Lab 6: ConfigMaps & Rollouts** | ConfigMap volume mount and `checksum/config: sha256sum` implemented. Verified that modifying HTML triggers automatic Pod replacement. | Clarified why the checksum must live under `spec.template.metadata.annotations`. | `lab-06-complete` |
| **Lab 7: Validation & Tests** | `values.schema.json`, test hook Pod, and `NOTES.txt` implemented. Schema rejects invalid replicas (`-1`) and ports (`70000`). | Fixed hook deletion race condition: updated policy to `before-hook-creation` so `helm test --logs` reliably fetches logs without "pod not found" errors. Added schema hint. | `lab-07-complete` |
| **Lab 8: Dependencies** | Companion subchart `lab-banner` created with `file://` dependency, `Chart.lock`, and global values propagation verified. | Added explicit failure recovery steps for missing `.tgz` archives using `helm dependency build`. | `lab-08-complete` |
| **Lab 9: Packaging & GitOps** | Bumped version to 0.2.0, packaged archive, and verified installation and testing from archive. | Added a zero-auth local OCI registry option (`docker run -d -p 5001:5000 ...`) so OCI publishing can be tested 100% offline without third-party registry accounts. | `lab-09-complete` |
| **Lab 10: Hooks & Recovery** | Pre-upgrade hook Job verified: failure blocks the upgrade and leaves the Deployment untouched; `--atomic` auto-rollback, `helm diff`, `kubectl apply --dry-run=server`, and `pending-upgrade` recovery via `helm rollback` all verified on k3d/kind, Helm v3.19.0. | Found that `helm upgrade --dry-run=server` did not report an invalid `imagePullPolicy`, so the lab teaches `helm template \| kubectl apply --dry-run=server`. Documented that a failed hook Job survives `helm uninstall`. | `lab-10-complete` |
| **Lab 11: Advanced Templating** | Scratch-chart experiments (`default`, `hasKey`, `ternary`, `coalesce`, `tpl`, `required`) and `lookup` token stability across upgrades verified. `lab-common` library chart with `file://` dependency, byte-identical label refactor (`diff` against pre-refactor render), `extraConfigMaps` deployed and `helm test` passed on k3d/kind, Helm v3.19.0. | Found `required` does not reject an empty map (renders an empty `data:`), so the library uses an explicit `fail`. Documented that a missing parent key is a nil-pointer error, not a `required` failure, and that library charts are not installable. | `lab-11-complete` |
| **Lab 12: Secrets, Signing & CI** | SOPS/age + `helm-secrets` deploy and rotation (Pod rolled by `checksum/secret`), 20 `helm-unittest` tests, `helm package --sign`/`helm verify`/`helm install --verify` incl. tamper and untrusted-key failures, OCI push + `helm pull --verify`, cosign sign/verify by digest, `ct lint --all`, `ct install` (3 `ci/` scenarios), and the `ct` version-bump gate verified on k3d/kind, Helm v3.19.0. The GitHub Actions workflow was YAML-linted only (not executed). | Found and documented: plain `helm -f` on an encrypted file silently deploys ciphertext; `helm package` bundles `secrets.*.yaml`/`.sops.yaml` unless ignored; an unanchored `tests/` in `.helmignore` drops the `helm test` hook from renders and packages; `ct` needs `values.yaml` in library charts and diffs against `origin/main`; helm-unittest needs `include`d templates listed and cannot test schema-blocked failures. | `lab-12-complete` |
| **Lab 13: Capstone** | `shop` umbrella (aliased `nginx-demo` + new `shop-api` PostgREST + `shop-db` PostgreSQL) installed as `shop-dev`, `shop-prod` (PVC), and `shop-oci` (signed, pulled from OCI with `--verify`); `helm test` through all three tiers; idempotent upgrade; failed post-upgrade hook (new manifests stay applied) and `--atomic` rollback; DB restart recovery; password-rotation trap and fix; 14 unit tests; `ct lint --all` and `ct install` for `shop` on k3d/kind, Helm v3.19.0. Argo CD part not executed. | Found and documented: PostgREST `/ready` stays 503 after a DB restart, so liveness now uses `/ready` behind a startup probe; rotating `POSTGRES_PASSWORD` does not change an initialized database; `app: <release>` label overlap makes the web Service select API pods (verified with EndpointSlices); nested `file://` dependencies must be built inner-first and break Git-only builds (Argo CD); post-hook failures leave new resources applied. | `lab-13-complete` |
| **Lab 14: Third-Party Charts** | External chart consumption (`podinfo/podinfo` v6.14.0 -> v6.15.0) verified: `helm repo add/search`, `helm show`, minimal delta values file, template inspection via `helm pull --untar`, `helm diff upgrade -n helm-lab`, `--post-renderer` with `kubectl kustomize` for corporate compliance annotations/labels, and `helm rollback` all executed on k3d/kind, Helm v3.19.0. | Found and documented: `helm diff upgrade` requires explicit `-n <namespace>` or it errors with "release not found"; many community charts lack `values.schema.json` so invalid types render silently unless caught by server dry-run (`kubectl apply --dry-run=server`); `--post-renderer` with `kubectl kustomize` eliminates chart forking. | `lab-14-complete` |
| **Lab 15: CRDs & Operators** | Native `crds/` lifecycle (`charts/crd-demo`), `helm template` vs `--include-crds`, silent field drop trap during un-upgraded CRD schema, 3-way merge desync mechanics, manual `kubectl apply` CRD upgrade playbook, `cert-manager` v1.16.2 operator install with `crds.enabled=true`, in-cluster Issuer and Certificate issuance to TLS Secret, safe uninstall retention via `helm.sh/resource-policy: keep`, and CRD ordering/hook traps verified on k3d, Helm v3.19.0. | Found and documented: `helm upgrade` completely skips `crds/`; Kubernetes silently strips unrecognized CR fields if cluster CRD is not updated; Helm release Secret records the stripped field, creating a 3-way merge patch desync where subsequent `helm upgrade` commands send no patch until values or templates change; `cert-manager` avoids this by putting CRDs in `templates/` with `"helm.sh/resource-policy": keep`. | `lab-15-complete` |
| **Lab 16: Production Hardening** | Enterprise hardening on `nginx-demo` (v0.6.0): unprivileged NGINX (UID 101, port 8080), Pod/container `securityContext` (`runAsNonRoot`, `RuntimeDefault`, `allowPrivilegeEscalation: false`, `capabilities.drop: [ALL]`), dynamic `emptyDir` mounts for `readOnlyRootFilesystem`, hardened test hooks and migration Jobs, `PodDisruptionBudget` (`minAvailable: 1`), `topologySpreadConstraints`, default resource requests/limits, `NetworkPolicy` ingress/egress, `helm-docs` markdown generation, and `kubeconform` OpenAPI schema validation (9/9 valid) verified on k3d, Helm v3.19.0. | Found and documented: unhardened test hook and migration Job pods fail admission in secure namespaces; `readOnlyRootFilesystem: true` requires writable `emptyDir` mounts on `/tmp`, `/var/cache/nginx`, and `/var/run`; an unhardened upgrade in a restricted namespace blocks ReplicaSet pod creation with context deadline exceeded while existing compliant pods remain healthy; NetworkPolicies with Egress require explicit UDP/TCP 53 DNS allow rules. | `lab-16-complete` |
| **Lab 17: Many Releases (Helmfile & ApplicationSet)** | Declarative multi-release orchestration via Helmfile v1.8.0 verified: multi-document `helmfile.yaml.gotmpl`, layered environment values (`environments/dev.yaml` and `prod.yaml`), dynamic Go templating in values files, DAG topological ordering via `needs:` (`backend-db` -> `backend-api` -> `frontend-web`), label filtering (`-l tier=backend`), environment promotion (`dev` to `prod` with replica/PDB/NetworkPolicy and probe v6.15.0 bumps), and Argo CD `ApplicationSet` with the `List` generator verified on k3d, Helm v3.19.0. | Found and documented: Helmfile v1 requires separating `environments:` and `releases:` with YAML multi-doc delimiters (`---`); files using Go template directives directly must use `.yaml.gotmpl` extension; `createNamespace: true` ensures clean bootstrap in empty namespaces; standalone `secret.create` in subcharts enables them to run independently in Helmfile while preserving compatibility with umbrella charts. | `lab-17-complete` |
| **Ext: Networking** | Ingress routing tested through k3s's built-in Traefik controller. | Added in-cluster curl test command (`kubectl run curl-test ...`) to verify Ingress routing on Docker/k3d networks, plus complete `ingress.yaml` template in hints. | `extension-networking-complete` |
| **Ext: Health & Scaling** | Liveness/readiness HTTP probes and HPA v2 autoscaling verified with cluster metrics-server. | Added hint showing conditional omission of `spec.replicas` when HPA is active. | `extension-health-complete` |
| **Ext: Identity & Secrets** | ServiceAccount creation, Role, RoleBinding, and `existingSecret` verified with `printenv` and `kubectl auth can-i`. | Added hint templates for `serviceaccount.yaml`, `rbac.yaml`, and `_helpers.tpl` helper. | `extension-identity-complete` |
| **Ext: Storage & Workloads** | Created standalone `charts/storage-demo` with PVC and Deployment. Verified data persistence across pod deletion. | Added hint templates and full working manifests for `storage-demo` on local-path storage. | `extension-storage-complete` |
